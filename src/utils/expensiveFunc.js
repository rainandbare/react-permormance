export default function expensiveFunction(arr) {
  for (let i = 1; i <= arr.length; i++) {
      for(let j = 1; j <= i; j++) {
        // eslint-disable-next-line
        const hello = 1 + 1
      }
    }
}

